# Web environment variables (for local development without Docker)
# Copy to .env.local in apps/web/ directory.
# These are inlined at build time (NEXT_PUBLIC_ prefix) — changing them
# requires a rebuild, not just a restart.

NEXT_PUBLIC_PUBLIC_BASE_URL=http://localhost:3000
NEXT_PUBLIC_API_ENDPOINT=http://localhost:5000/api
