# Root .env — used by docker compose (infra/docker-compose.yml reads ${IMGUR_CLIENT_ID})
# Copy to .env and fill in values before running `docker compose up`.

# Imgur image hosting (optional — predictions work without it, image_url will be null)
IMGUR_CLIENT_ID=
